services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - app_uploads:/usr/src/app/uploads
      - app_downloads:/usr/src/app/downloads

volumes:
  app_uploads:
  app_downloads:
